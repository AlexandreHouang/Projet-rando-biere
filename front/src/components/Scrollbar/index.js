// == Import npm
import React from 'react';

// == Import
import './scrollbar.scss';

// == Component
const Scrollbar = () => (
  <div className="scrollbar">
    <p>scrollbar</p>
  </div>
);

// == Export
export default Scrollbar;
